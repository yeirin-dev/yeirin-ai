# Gotenberg - DOCX → PDF 변환 서비스
# yeirin-ai는 네이티브 + PM2로 실행

services:
  gotenberg:
    image: gotenberg/gotenberg:8
    container_name: gotenberg
    ports:
      - "3001:3000"
    environment:
      - GOTENBERG_API_TIMEOUT=60s
      - GOTENBERG_LIBREOFFICE_DISABLE_ROUTES=false
    restart: unless-stopped
